{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Test",
            "type": "cppdbg", // Use "lldb" for macOS/LLDB
            "request": "launch",
            "program": "${workspaceFolder}/build/MWP/tests/Matrix", // Path to your test
            "args": [], // Add command-line args if needed
            "cwd": "${workspaceFolder}",
            "environment": [],
            "stopAtEntry": false,
            "MIMode": "gdb" // Use "lldb" for macOS
          }
    ]
}